aula56 template string

que seria uma nova forma de trata string

é normal que nos faz isso:

const curso = 'java script'
const canal = ' cfb curso'
const frase = ''o curso é' + curso + 'do canal' + canal;

caixa.innerhtml = frase




só que acaba nos tendo dificuldades no sentido de tempo, concatenar, espações, add quebra linha e etc, mas o código ta certo

por isso nos vamos aprender e usar template string - uma forma que facilita isso
e como funciona:

vamos entender com as regras e colocar em pratica


1 vamos trocar  o marcador da nossa string

então em vez de aspa nos usaramos crase '     ``     '

então:


const curso = 'java script'
const canal = ' cfb curso'
const frase = `este é o curso de`



e para adicionar a constante neh que é uma das principais, nos vamos usar ${curso}   -  cifrão e chaves com o nome da constante ou variável dentro, então:


const frase = `este é o curso de  ${curso} do canal  ${canal}`

mais pratico e eficiente tbm

para dar 'quebra de linha' é só da 'space' ele entende isso
claro que:

nos pode usar tbm <br/> ou \n

a diferença é que o 

\n quebra no console.log e o
<br/> no doom

pq o\n  não é doom


agr vamos criar uma lista como treino:

const carros = ['mazda', 'porche', 'toyota']

let ol = `<ol>`
carros.map((el)=>{
  ol+= `<li> ${el} </li>`
})

ol+ `</ol>`